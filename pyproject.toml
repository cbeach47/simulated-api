[project]
name = "simulated-api"
version = "0.1.0"
description = "FastAPI server that simulates various APIs for testing"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
]
